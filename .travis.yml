language: ruby
os: osx
env: OSX=10.11
osx_image: xcode8.0
rvm: system

before_install:
  - export HOMEBREW_DEVELOPER="1"
  - sudo chown -R $USER "$(brew --repo)"
  - git -C "$(brew --repo)" reset --hard origin/master
  - brew update || brew update
  - rm -rf "$(brew --repo $TRAVIS_REPO_SLUG)"
  - mkdir -p "$(brew --repo $TRAVIS_REPO_SLUG)"
  - rsync -az "$TRAVIS_BUILD_DIR/" "$(brew --repo $TRAVIS_REPO_SLUG)"
  - export TRAVIS_BUILD_DIR="$(brew --repo $TRAVIS_REPO_SLUG)"
  - cd "$(brew --repo)"
  - ulimit -n 1024

# Test installing packages works.
script:
  - brew test-bot --ci-auto wget

notifications:
  email:
    on_success: never
    on_failure: always
